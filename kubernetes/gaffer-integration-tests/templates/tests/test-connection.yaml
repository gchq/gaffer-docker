apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gaffer-integration-tests.fullname" . }}-test-connection"
  labels:
    {{- include "gaffer-integration-tests.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gaffer-integration-tests.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
