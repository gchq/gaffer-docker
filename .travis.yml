os: linux
language: minimal
env:
  - KUBERNETES_VERSION=`curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt` CHANGE_MINIKUBE_NONE_USER=true
before_install:
  - sudo cd/before_install.sh
install:
  - sudo cd/install.sh
script:
  - sudo cd/verify.sh
